version: '3.8'
services:
  hibernate-postgresql:
    image: postgres:14.5
    environment:
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 127.0.0.1:5432:5432